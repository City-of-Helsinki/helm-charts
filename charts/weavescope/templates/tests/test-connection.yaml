apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "weavescope.fullname" . }}-test-connection"
  labels:
    {{- include "weavescope.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "weavescope.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
